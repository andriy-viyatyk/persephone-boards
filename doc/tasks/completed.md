# Completed Tasks

Completed board tasks, newest first. One row per task — a compressed evidence summary that
survives after the task folder is deleted.

| ID | Title | Board | Notes |
|----|-------|-------|-------|
| BT-021 | Move the `greek-gods.fg.json` example into the Force Graph board | `force-graph` v1.0.0 | The 26 KB / 63-node / 87-link Greek-mythology sample graph moved out of the app (`assets/guides/examples/`, where it was orphaned — no live reference anywhere in `assets/`, `qa/` or `src/`) into `boards/force-graph/examples/greek-gods.fg.json`, so it ships and versions with the board. Linked from the board's own guides (BT-019), so a user who installs the board has something to open in one click. The app's copy is deleted by US-1405 during the Graph cleanup. |
| BT-020 | Todo board — backfill its documentation | `todo` v1.2.0 | The built-in Todo editor and its 157-line guide were deleted in Persephone commit `a2692189`, leaving the board shipping undocumented. Backfilled `boards/todo/guides/` (`index.md`, `editor.md`, `agent.md`) declared via `"guides": "guides"` — documenting **the board as implemented**, not the historical `todo-view` editor: the toolbar/item-list/lists/tags/search UI, the `.todo.json` root shape and all eight item fields, and the AiVision surface (thirteen published methods, seven declared elements) read out of `app.js` rather than translated from the app's old text. `editorId: "board"` on the user guide only (see BT-019). Published as 1.2.0. |
| BT-019 | Force Graph board — ship its own user and agent documentation | `force-graph` v1.0.0 | Graph documentation moved out of the app (`assets/guides/editors/graph.md` 8.1 KB, `assets/guides/formats/graph.md` 9.4 KB) into `boards/force-graph/guides/` — four pages (`index.md`, `editor.md` user, `format.md` both, `agent.md` agent) declared with `"guides": "guides"`, so it ships and versions with the board and is discoverable by users (About page, F1) and agents (`guides.search()`). Depends on app **US-1406** (the `guides` manifest field + multi-source guide index). **`editorId: "board"` on `editor.md` only** — the literal self-claim token (`BOARD_SELF_EDITOR_ID`), honored only under the board's own mount, since a board's real editor id (`board-editor:<absolute root>`) is machine-specific; F1 must land on the user guide, so no second page claims it. **API text verified against code, not prose:** all 33 element names and 83 model members scripted-cross-checked against `graph-aivision.js`, format rules against `graph-core.js`, plus live MCP spot-checks. `qa/surfaces/editors/graph.md` deliberately **retired, not ported** — it tests the app's `graph-view` MCP descriptors, which the board's `pages[i].editor.app` surface does not share. |
| BT-018 | Force Graph board — colored tab icon | `force-graph` v1.0.0 | The board icon rendered black-on-black: a board icon is loaded as an `<img>`, so `currentColor` inherits nothing and resolved to near-black in the page tab. `icon.svg` switched to fixed colors (`#7A93A8` strokes, `#29B6F6`/`#1BA1E2`/`#B07CE8` nodes) that read on both light and dark themes — the same fix the PDF board took (Acrobat red) and now the convention for every board icon. |
| BT-017 | Force Graph board — detail panel resizer | `force-graph` v1.0.0 | Owner-reported during hands-on testing: the properties panel had no resize affordance and could not be enlarged. **Two defects, not one** — BT-016 had recorded this as an "accepted difference" and was wrong. (1) No affordance: a 4px transparent left-edge strip visible only on hover, replaced with the built-in's 12×12 three-stroke corner grip (`createResizeGripElement()` in `graph-ui.js`, `sw-resize`, opacity 0.4 to 0.9, strokes from `--p-text-muted`). (2) It could not grow **at all**: the drag clamped against `root.parentElement`, but the parent is `#detail-slot`, an absolutely-positioned wrapper that **shrink-wraps its content**, so the clamp collapsed onto the current size — now clamps against the **canvas** rect. Added `state.height` (300 default) and two-axis drag with `MIN_WIDTH/MIN_HEIGHT/MAX_PERCENT` mirroring the built-in; dropped the CSS `max-height: 60vh` that fought the JS clamp. Verified live against `gg-board.fg.json`: grow 260×300 to 400×460 exact, max 1422×863 = exactly 90% of the 1580×959 canvas on both axes, min 200×200, collapse/re-expand preserves the chosen size, properties grid no longer overflows, `ui.log` clean. **Noted, not changed:** `graph-panels.js` holds a literal NUL byte at offset 30845 (a deliberate string delimiter) — legal JS, but it makes `file`/`grep` treat the file as binary. |
| BT-016 | Force Graph board — parity verification against the built-in editor | `force-graph` v1.0.0 | Both editors driven side by side in the running app over the EPIC-100 §1.3 feature inventory, with an edge-case fixture (groups, `maxVisible`, badges, duplicate ids, custom props, markdown links, `#N` keys, unknown top-level keys) and colors compared across more than one theme. **Round-trip output diffed between the two editors — byte-identical.** Drove a chrome restyle to match the built-in: canvas-first layout with a floating `.fg-chrome` card, the built-in's six toolbar SVGs ported into a new `graph-icons.js`, toolbar reordered. Accepted differences recorded (notably `av-grid`'s `showMenu` for all menus — nested submenus, separators, disabled/invisible items verified in-frame — and the Extract JSON-page fallback, which only matters after US-1405). **Untestable without a human:** real pointer gestures, Ctrl+S from a real keypress, and hand drag-resizing the detail panel — the last of which is exactly where BT-017's two defects were hiding. Synthetic-input gotcha worth keeping: `PointerEvent`s dispatched without `view: window` produce a spurious d3 "Cannot read properties of null (reading 'document')" in `ui.log`, and without `pointerdown` context menus stack. |
| BT-015 | Force Graph board — panels, grids, menus, dialogs, AiVision surface (v2) | `force-graph` v1.0.0 | Completed the board to full parity over the EPIC-100 §1.3 checklist, filling the seams v1 left (`#panel-slot`, `#legend-slot`, `#detail-slot`, four null renderer callbacks, `window.FG.app`): detail panel with Info/Properties/Links tabs and two editable grids; legend panel (Selection/Level/Shape) with descriptions persisted in the file; Physics / Expansion / Results panel tabs; four context menus plus the selection menu; the remaining dialogs; hover tooltip with Copy as Markdown; the two image-export buttons (EPIC-100 **D4** — they live in the board's own toolbar); the five cross-editor page-creation features (Markdown page, JSON grid, Extract / Extract with children, open as a Drawing); and the full agent surface at `pages[i].editor.app`. **D3** holds: no agent-facing method blocks on an overlay (the `*Core` split). |
| BT-014 | Force Graph board — models, renderer, canvas, content host (v1) | `force-graph` v0.1.0 | v1 of the port of the app's built-in `graph-view` editor for `.fg.json` (`src/renderer/editors/graph/`, 29 files / 8,569 LOC — ~2,800 identified as framework-free and portable near-verbatim: six model classes, `shapeGeometry`, `types`, `ForceGraphRenderer`). Delivered the pure model layer, the canvas renderer, the d3-force simulation and the content-host file I/O, and doubled as **the feasibility spike: yes, a continuous physics simulation runs fine inside a board iframe.** Six shapes, five levels, group containers, distinct root node; pan/wheel-zoom/drag, click + Ctrl+click selection, Shift neighbour highlight, Alt+click to toggle a link; BFS visibility for large graphs (hidden-neighbour badges, Ctrl+click deep expand, Expand all); search with canvas highlighting; unknown keys preserved on save. Left explicit seams for BT-015. |
| BT-013 | Excel Viewer — large-workbook load time | `excel-viewer` v1.1.1 | 20.5 MB / 124k-row workbook: **10.9 s to 6.0 s (~45%)**, sheet switching 2.0 s to 0.4 s. Two changes: workbooks over 4 MB parse **only the sheet being viewed**, the rest lazily on first open; and the file is read as **raw bytes instead of base64** — which also lifted a hard ceiling, since base64 of a >~400 MB workbook exceeded the browser's maximum string length and could not be opened at all. Raw-bytes reading requires Persephone 4.0.21 (`minAppVersion` bumped). |
| BT-006 | PDF Viewer board (.pdf) | `pdf-viewer` v1.0.0 → v1.0.2 | Board side of Persephone **EPIC-047** — moved the built-in PDF editor out of the app so ~21 MB of vendored pdf.js leaves the installer. The board hosts the **same stock pdf.js `viewer.html`** the built-in editor did (search, thumbnails, outline, zoom/fit, rotate, print), not a reimplementation; opens a local file, a PDF inside an archive (`archive.zip!doc.pdf`), and an `http(s)` URL. Needs `frame-src` + `wasm-unsafe-eval` in `BOARD_CSP` and the `editorSources: "any"` gate, so 1.0.0 was published **ahead of Persephone 4.0.18** deliberately — `isCompatible` kept it uninstallable until that app release, then it appeared with no further action. Parity confirmed against the built-in (US-909) including the two open questions: **print and Save-as from inside the `board://` frame both work**, no bridge support or CSP change needed. Since: 1.0.1 screenshot, 1.0.2 raw-bytes read (>400 MB files, Persephone 4.0.21). **Gotchas:** the publish script writes a *fixed field set* into `boards-manifest.json`, so `editorSources`/`editorPriority` never appear in the catalog — they travel in the ZIP's own `board-manifest.json`, which is what the custom-editor registry actually reads; a `board-manifest.json` edit does not take effect on board reload (manifests are cached until a trust change or restart); and `app.openRawLink(linkData)` does nothing from a script — the working call is `app.events.openRawLink.sendAsync(io.createLinkData(href))`. **Left open:** the `editorPriority` 100 drop, blocked on Persephone US-911 removing the built-in editor. |
| BT-005 | SQLite Viewer board (.db/.sqlite/.sqlite3/.db3) | `sqlite-viewer` v1.0.0 | Read-only SQL browser; **first consumer of `persephone.executeNode()`** (US-882, hence `minAppVersion: 4.0.16`): a **resident query server** (`scripts/db-server.mjs`) on Persephone's bundled Node — `node:sqlite` readOnly, JSON lines over stdin/stdout ({id,op,sql} → {id,columns,rows,ms}), rows as ARRAYS (column order + dup names), 20k row cap via `stmt.iterate()`. **Tables list = secondary view** (`tables.html`, Persephone sidebar panel) synced via `persephone.state.*` (`db`/`selected` down, `run:{name,seq}` commands up). Bundles **sqlite-vec v0.1.9** (`lib/vec0.dll`, MIT, win-x64, 283 KB; `allowExtension` + best-effort `loadExtension`, re-disabled after) so `vec0` tables work incl. **KNN MATCH**. Tabulator grid (natural sort, header filters, range select + own TSV copy — Tabulator clipboard dead in board iframe). Stop = kill+respawn server (no cross-process SQLite cancel); `runQuery` ignores internal cancel-reason rejections. Verified live on a real mneme `index-v2.db`: FTS5 MATCH+snippet 3-table JOIN, readonly DELETE rejection, vec KNN (top hit distance 0), 20k truncation, cancel flow, `ui.log` clean. Also drove **US-882 + US-883** (`--p-bg-dark`/`--p-hover`/`--p-tree-selection` board tokens) in the app repo; chrome restyle swept all 5 published boards (drawio 1.0.3, excel/word/ppt/pe 1.0.1). **Gotcha:** the resident server locks `lib/vec0.dll` + holds the board folder as cwd — kill it (`stopServer` is a global) before git operations that touch the folder. |
| BT-004 | PE Viewer board (.exe/.dll/.sys/.ocx/.scr) | `pe-viewer` v1.0.0 | Read-only Windows PE inspector. **Hand-rolled offline PE parser** (`pe-parser.js`, `DataView`-based, each sub-structure `safe()`-wrapped) — no third-party PE lib; only vendored **MD5** (`lib/md5.js`, RFC 1321) for imphash + file MD5 (SHA-1/256 via `crypto.subtle`). 8 tabs: Overview (icon + version/company/copyright, type, security-mitigation chips ASLR/DEP/CFG/HEVA/SafeSEH, fingerprints) · Headers (COFF/optional + data dirs) · Sections (perms + per-section entropy meters) · Imports (per-DLL accordion) · Exports · Signature (Authenticode presence + best-effort PKCS#7 commonName scan) · Hashes (MD5/SHA-1/SHA-256/imphash) · Details (version strings, manifest XML, PDB path, Rich header, packer hints). Verified live across PE32+/PE32, native/.NET, exe/dll: kernel32.dll (1693 exports, signed), explorer.exe (icon+manifest+rich+PDB), notepad.exe (correctly "Unsigned" — catalog-signed), SysWOW64 kernel32 (PE32 x86), a .NET assembly (CLR detect). End-to-end association confirmed (opening notepad.exe → `board-editor:...pe-viewer`); `ui.log` clean (offline, no CSP). `editorPriority: 100` (PE exts unclaimed by any built-in — only Monaco@0). **Two offset bugs found & fixed in dev:** `NumberOfRvaAndSizes` (+LoaderFlags is uint32, not 8B) and section Characteristics (offset +36, not +24). |
| BT-003 | PowerPoint Viewer board (.pptx) | `powerpoint-viewer` v1.0.0 | Read-only viewer: **pptx-preview 1.0.7** (ISC; self-contained UMD bundling JSZip/echarts/lodash — chosen over PPTXjs to avoid jQuery + URL-fetch input) renders all `.pptx` slides to HTML. Slides stack in a scroll view scaled to board width via CSS `zoom` (ResizeObserver), with a slide counter + prev/next + Arrow/PageUp-Down keys. Verified live via screenshots: 7-slide deck, bold headings, bullets, embedded image (data: URL); counter sync 1/7→4/7; clean empty state; `ui.log` clean (offline, no CSP). `editorPriority: 200` (zip-based `.pptx` needs > 100 to beat archive-view — BT-001/002 trap). Fidelity limits (documented): no animations, fixed 960×540 viewport so non-16:9 decks scale approximately; legacy `.ppt` out of scope. |
| BT-002 | Word Viewer board (.docx) | `word-viewer` v1.0.0 | Read-only viewer: docx-preview 0.4.0 (+ JSZip 3.10.1) renders `.docx` OOXML **page-accurately** ("looks like Word") into a scrollable `#doc` container. Headings, bold/italic, bullet + numbered lists, tables, and embedded images (inlined as `data:` URLs via `useBase64URL`) all verified live via screenshot; clean empty state; `ui.log` clean (offline, no CSP). Pages kept authentic white paper, only the "desk" themed. `editorPriority: 200` (zip-based `.docx` needs > 100 to beat archive-view — same trap as BT-001). Legacy `.doc` out of scope; no virtualization (whole doc in DOM) — v1 limits. |
| BT-001 | Excel Viewer board (.xlsx / .xls) | `excel-viewer` v1.0.0 | Read-only viewer: SheetJS 0.20.3 parses `.xlsx`+`.xls` → Tabulator 6.5.1 grid. Excel-style (column letters + row numbers), formatted dates/cached formula values, numeric-aware sort, per-column filter, range-select + Copy (right-click menu **and** Ctrl/Cmd+C via own `copySelection`, since Tabulator's clipboard is dead in the board iframe), virtualized large sheets, no zebra striping. Tested live in Persephone. **Key finding:** zip-based types need `editorPriority > 100` to beat the built-in archive viewer (applies to BT-002/BT-003). |
