# What's New — Todo

One line per change, newest first. Keep it short. Record pending changes under a heading for
the **next version** you'll release (the version `board-manifest.json` will be bumped to).

## 1.0.1

- Now the default editor for `.todo.json` files — they open in the board and show the Todo icon in the file tree.

## 1.0.0

- Todo list editor for `.todo.json` files — a content-host board that opens alongside the built-in editor with a **Lists & Tags** sidebar.
- Quick-add, per-item comments, colored tags, and done/undone with a "Done" separator.
- Search with matched-word highlighting, a header **List:** switcher, and the item count in the footer.
